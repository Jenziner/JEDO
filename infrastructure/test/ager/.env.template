# ========================================
# JEDO Infrastructure - Root Config
# ========================================
# !!! ANLEITUNG für AGER !!!
#
# 1. Diese Datei nach .env kopieren
# 2. Harbor-Credentials anpassen (von REGNUM erhalten)
# 3. Image-Tags prüfen (normalerweise :latest)
# 4. Ausführen: ./harbor-login.sh
# 5. Infrastruktur starten: docker compose pull && docker compose up -d
# ========================================

# ========================================
# Harbor Registry
# ========================================
HARBOR_REGISTRY=harbor.jedo.me
HARBOR_PROJECT=services

# !!! ANPASSEN !!!
# Credentials von REGNUM erhalten
HARBOR_USER=harbor-username
HARBOR_PASS=harbor-password

# ========================================
# Docker Images (Harbor)
# ========================================
# Normalerweise kein Änderung nötig - :latest verwenden
GATEWAY_IMAGE=${HARBOR_REGISTRY}/${HARBOR_PROJECT}/jedo-gateway:latest
CA_SERVICE_IMAGE=${HARBOR_REGISTRY}/${HARBOR_PROJECT}/ca-service:latest
LEDGER_SERVICE_IMAGE=${HARBOR_REGISTRY}/${HARBOR_PROJECT}/ledger-service:latest

# ========================================
# Docker Network
# ========================================
DOCKER_NETWORK_NAME=jedo-ecosystem

# ========================================
# User/Group (für file permissions)
# ========================================
# Linux: Nutze deine User-ID (id -u)
UID=1000
GID=1000

# ========================================
# Service Ports (Defaults)
# ========================================
# Nur ändern bei Port-Konflikten
GATEWAY_PORT=3000
CA_SERVICE_PORT=3001
LEDGER_SERVICE_PORT=3002
